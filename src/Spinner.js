import React from 'react';
import './Spinner.css'; // You can create a separate CSS file for styling

const Spinner = () => {
  return (
    <div className="spinner">
      <div className="spinner-inner"></div>
    </div>
  );
};

export default Spinner;
